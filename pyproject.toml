[project]
name = "bpa-vision"
version = "0.1.0"
description = "Universal video analytics platform for on-prem/edge deployment"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
